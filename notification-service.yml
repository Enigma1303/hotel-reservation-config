spring:
  datasource:
    url: jdbc:mysql://localhost:3310/notification_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9094
    consumer:
      group-id: notification-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        spring.json.use.type.headers: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: notificationtester612@gmail.com
    password: stsp stgs cqqj hdtg
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: false
    from: notificationtester612@gmail.com

notification:
  retry:
    max-attempts: 3
    interval: 60000